get = (self, invite_id: @string) {
    return $.http_request(
        "get",
        "https://discord.com/api/v9/invites/{}".fmt(invite_id),
        {},
        "",
    )
}

delete = (self, invite_id: @string) {
    return $.http_request(
        "delete",
        "https://discord.com/api/v9/invites/{}".fmt(invite_id),
        self.headers(),
        "",
    )
}

return {
    get,
    delete,
}
